/var/log/epoptes.log {
	create 640 root adm
	missingok
	nomail
	rotate 5
	weekly
	postrotate
		if [ -f /var/run/epoptes.pid ]; then
			kill -USR1 "$(cat /var/run/epoptes.pid)" 2>/dev/null || true
		fi
	endscript
}
